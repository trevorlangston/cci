def find_successor(node):
    pass


if __name__ == "__main__":
    pass

"""
if node.right:
    # A node can either be the left child or the right child of it's parent.
    # If it is the left child, then the node's right child must be greater then
    # the node, and less than the node's parent. If it is the right child, then
    # the nodes right child must be greater then it and its parent. In either
    # case, if the node has a right child, we know the answer lies on that
    # branch.
else:
    # traverse up the branch as long as the node is the right child of its parent.
    # As soon as it is not, we can return the parent.
"""
